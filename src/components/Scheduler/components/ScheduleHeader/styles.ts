import { tv } from 'tailwind-variants';

export const wrapperCss = tv({
  base: 'bg-primary-blue text-white flex flex-col items-center p-2 rounded-sm',
});

export const titleCss = tv({
  base: 'text-2xl font-bold pb-1',
});

export const subtitleCss = tv({
  base: 'bg-primary-blue text-white flex flex-col items-center',
});
