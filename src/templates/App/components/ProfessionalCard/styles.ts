import { tv } from 'tailwind-variants';

export const profileWrapperCss = tv({
  base: 'flex gap-8',
});

export const professionalNameCss = tv({
  base: 'text-2xl text-gray-600 font-bold',
});

export const professionCss = tv({
  base: 'text-base text-primary-blue font-bold uppercase',
});

export const reviewsWrapperCss = tv({
  base: 'flex gap-4 items-center',
});

export const reviewrsCss = tv({
  base: 'text-sm text-gray-400',
});

export const serviceDetailsCss = tv({
  base: 'text-gray-600 font-bold',
});

export const descriptionCss = tv({
  base: 'text-gray-600 mt-4',
});
